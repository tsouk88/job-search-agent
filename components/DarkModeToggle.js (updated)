import React from 'react';
import { Switch } from '@mui/material';

const DarkModeToggle = ({ handleToggle, darkMode }) => {
  return (
    <Switch
      checked={darkMode}
      onChange={handleToggle}
      name="darkMode"
      color="primary"
    />
  );
};

export default DarkModeToggle;